# 2026 Publications Update

## Scope

Add three August 2026 arXiv preprints to the Publications section of the personal homepage:

1. NCGR: Noise-Conditional Gated Rectification for Camera Extrinsic Perturbations in BEV 3D Object Detection
2. CrossScope: A Role-Asymmetric World Model for Joint Dual-Scope Surgical Video Prediction
3. EndoWAM: A Grounded World-Action Model for Generalizable Endoscopic Navigation

## Presentation

Each paper will use the existing two-column publication layout and appear above the existing entries. The left column will show a pipeline overview figure extracted from its PDF. The right column will contain the full author list, with Wanhao Liu highlighted, the year and arXiv status, title linking to the arXiv abstract page, links to the PDF and arXiv page, and an expandable abstract.

## Assets and Data

The pipeline figures will be saved below `images/` in one directory per paper. Titles, authors, abstracts, PDF URLs, and arXiv URLs will match the official arXiv records. Existing publications and styling remain unchanged.

## Verification

Validate the HTML structure and link targets, inspect the local page at desktop and mobile widths, confirm all three figures render, then commit and push the update to the configured `Wanhao-Liu` remote.

## CrossScope Media Revision

Replace the CrossScope pipeline image with the project's subtitled teaser video at `https://wanhao-liu.github.io/CrossScope/static/videos/final_en_subtitled_compact.mp4`. Preserve the project's muted, looping, autoplaying, and user-controllable playback behavior. Add a `webpage` link to `https://wanhao-liu.github.io/CrossScope/`.

Use a shared preview container for the three new publication entries so images and video occupy a consistent visual height while retaining their full aspect ratios. Verify the revised media presentation and link behavior at desktop and mobile widths.
